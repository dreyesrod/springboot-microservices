eureka:
    instance:
        instance-id: ${spring.application.name}:${random.uuid}
management:
    endpoints:
        web:
            exposure:
                include: '*'
server:
    port: 0
spring:
    application:
        name: product-microservice
        zipkin: http://localhost:9411
    cloud:
        config:
            profile: dev
            uri: http://localhost:8888
        vault:
            application-name: product-microservice
            authentication: TOKEN
            host: localhost
            port: 8200
            scheme: http
            token: hvs.c1mHXlUH13fC5YousBT78MZI
    rabbitmq:
        host: localhost
        password: guest
        port: 5672
        username: guest
    security:
        oauth2:
            resourceserver:
                jwt:
                    issuer-uri: http://localhost:9090/realms/springboot-microservices-realm
